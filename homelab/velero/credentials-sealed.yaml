# To generate:
# 1. Create a Garage access key for Velero (via the Garage admin UI or CLI)
# 2. Create the velero-backups bucket in Garage
# 3. Run:
#    kubectl create secret generic velero-credentials -n velero \
#      --from-literal=cloud="[default]
#    aws_access_key_id=<GARAGE_ACCESS_KEY>
#    aws_secret_access_key=<GARAGE_SECRET_KEY>" \
#      --dry-run=client -o yaml | \
#      kubeseal --kubeconfig /etc/rancher/k3s/k3s.yaml --format yaml \
#      > homelab/velero/credentials-sealed.yaml

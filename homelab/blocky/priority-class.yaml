apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: dns-critical
# Higher than database-critical (1000000) — no DNS means nothing in the cluster works
value: 1100000
globalDefault: false
description: "Critical priority for DNS pods — must survive node memory pressure"

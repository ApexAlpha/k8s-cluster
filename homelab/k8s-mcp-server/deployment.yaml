apiVersion: apps/v1
kind: Deployment
metadata:
  name: k8s-mcp-server
  namespace: mcp
  labels:
    app: k8s-mcp-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: k8s-mcp-server
  template:
    metadata:
      labels:
        app: k8s-mcp-server
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      serviceAccountName: k8s-mcp-reader
      containers:
      - name: server
        image: ghcr.io/containers/kubernetes-mcp-server:latest@sha256:9fb3da5be9a6dfd9d134dfea59b72e31e71405eec5027af7f8cb9d7a930b17eb
        imagePullPolicy: IfNotPresent
        args:
        - --port
        - "8080"
        - --cluster-provider
        - in-cluster
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 256Mi
        startupProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
          failureThreshold: 12
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8080
          periodSeconds: 10

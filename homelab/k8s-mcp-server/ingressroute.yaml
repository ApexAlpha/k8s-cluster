apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: k8s-mcp-server
  namespace: mcp
spec:
  entryPoints:
  - websecure
  routes:
  - kind: Rule
    match: Host(`mcp.k8s.hu.ls`)
    services:
    - name: k8s-mcp-server
      port: 8080
  tls:
    certResolver: letsencrypt

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-4k-config-local
  namespace: media
  annotations:
    argocd.argoproj.io/sync-options: SkipHealthCheck=true
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sonarr-4k
  namespace: media
  labels:
    app: sonarr-4k
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sonarr-4k
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: sonarr-4k
    spec:
      tolerations:
        - key: "node.kubernetes.io/unreachable"
          operator: "Exists"
          effect: "NoExecute"
          tolerationSeconds: 30
        - key: "node.kubernetes.io/not-ready"
          operator: "Exists"
          effect: "NoExecute"
          tolerationSeconds: 30
      containers:
        - name: sonarr
          image: lscr.io/linuxserver/sonarr:latest
          ports:
            - name: http
              containerPort: 8989
          env:
            - name: PUID
              value: "1000"
            - name: PGID
              value: "1000"
            - name: TZ
              value: "Europe/Amsterdam"
            # External auth — trust Authelia's Remote-User header, skip Sonarr's own login
            - name: SONARR__AUTH__METHOD
              value: "External"
            - name: SONARR__AUTH__REQUIRED
              value: "DisabledForLocalAddresses"
            # PostgreSQL configuration
            - name: SONARR__POSTGRES__HOST
              value: "postgres-shared-pooler.database.svc"
            - name: SONARR__POSTGRES__PORT
              value: "5432"
            - name: SONARR__POSTGRES__USER
              value: "app"
            - name: SONARR__POSTGRES__PASSWORD
              valueFrom:
                secretKeyRef:
                  name: sonarr-4k-db-credentials
                  key: password
            - name: SONARR__POSTGRES__MAINDB
              value: "sonarr4k-main"
            - name: SONARR__POSTGRES__LOGDB
              value: "sonarr4k-log"
          volumeMounts:
            - name: config
              mountPath: /config
            - name: media
              mountPath: /data
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 2000m
              memory: 1Gi
          startupProbe:
            httpGet:
              path: /ping
              port: http
            failureThreshold: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /ping
              port: http
            periodSeconds: 30
            failureThreshold: 5
            timeoutSeconds: 10
          readinessProbe:
            httpGet:
              path: /ping
              port: http
            periodSeconds: 10
            failureThreshold: 3
            timeoutSeconds: 5
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: sonarr-4k-config-local
        - name: media
          persistentVolumeClaim:
            claimName: media-data
---
apiVersion: v1
kind: Service
metadata:
  name: sonarr-4k
  namespace: media
spec:
  selector:
    app: sonarr-4k
  ports:
    - name: http
      port: 80
      targetPort: 8989
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: sonarr-4k
  namespace: media
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`sonarr-4k.k8s.hu.ls`) && PathPrefix(`/api`)
      kind: Rule
      priority: 20
      services:
        - name: sonarr-4k
          port: 80
    - match: Host(`sonarr-4k.k8s.hu.ls`)
      kind: Rule
      priority: 10
      middlewares:
        - name: authentik
          namespace: authentik
      services:
        - name: sonarr-4k
          port: 80
  tls:
    secretName: wildcard-k8s-hu-ls-tls

apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: trawl
  labels:
    app: redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
        - name: redis
          image: redis:8.10-alpine@sha256:ba6e394f6acc2a695ef1b6944f161b9ca813711739be68319fa0db3470673f1d
          ports:
            - name: redis
              containerPort: 6379
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 512Mi
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          emptyDir: {}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: trawl
  namespace: trawl
  labels:
    app: trawl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trawl
  template:
    metadata:
      labels:
        app: trawl
    spec:
      nodeSelector:
        kubernetes.io/hostname: k3s-home
      imagePullSecrets:
        - name: ghcr-pull-secret
      # Camoufox/Firefox needs to write runtime files across /opt/camoufox;
      # running as non-root hangs browser launch (proven: worked as root on nuc).
      securityContext:
        runAsUser: 0
        runAsGroup: 0
      containers:
        - name: trawl
          image: ghcr.io/germondai/trawl:latest@sha256:63966602c3168c0c30d2cb42bb620edd8b813d4dd9f71fe2cc75c747acaa6bde
          imagePullPolicy: IfNotPresent
          # Camoufox/Firefox needs user namespaces for its sandbox; the default
          # k3s seccomp profile blocks unshare -> browser launch times out.
          securityContext:
            seccompProfile:
              type: Unconfined
          ports:
            - name: http
              containerPort: 8191
          env:
            - name: REDIS_URL
              value: "redis://redis:6379"
            - name: BROWSER_POOL_SIZE
              value: "1"
            - name: BROWSER_CONTENT_PROCESSES
              value: "2"
            - name: PORT
              value: "8191"
            - name: TZ
              value: "Europe/Amsterdam"
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: "4"
              memory: 4Gi
          startupProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            failureThreshold: 30
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            failureThreshold: 12
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 30
            failureThreshold: 10
            timeoutSeconds: 5
          volumeMounts:
            - name: shm
              mountPath: /dev/shm
            - name: addons
              mountPath: /opt/camoufox/addons
      volumes:
        - name: shm
          emptyDir:
            medium: Memory
            sizeLimit: 1Gi
        - name: addons
          emptyDir: {}

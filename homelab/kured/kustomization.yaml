apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# NOTE: application.yaml is intentionally NOT listed here — the
# root-homelab app (homelab/kustomization.yaml) owns the kured
# Application CR. Same pattern as system-upgrade.

resources:
  - kured.yaml